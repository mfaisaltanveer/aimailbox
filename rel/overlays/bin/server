#!/bin/sh
set -eu

cd -P -- "$(dirname -- "$0")"

echo "Creating database..."
./aimailbox eval "Aimailbox.Release.create"

echo "Running migrations..."
./aimailbox eval "Aimailbox.Release.migrate"

echo "Starting server..."
PHX_SERVER=true exec ./aimailbox start
